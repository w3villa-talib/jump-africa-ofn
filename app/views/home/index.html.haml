:javascript
  localStorage.setItem("jwt_token", "#{session[:jwt_token]}"); 
  localStorage.setItem("jumpAfrica_user_id","#{session[:jumpAfrica_user_id]}")
  

- content_for :page_alert do
  = render "shared/menu/alert"

%div{"ng-controller" => "HomeCtrl"}
  = render "home/tagline"

  #panes
    = render "home/brandstory"
    = render "home/system"
    = render "home/cta"
    = render "home/stats"

  = render "shared/footer"
